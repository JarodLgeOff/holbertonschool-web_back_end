#!/usr/bin/env python3
"""Task 2: Floor a float with annotations."""


def floor(n: float) -> int:
    """Returns the floor of a float as an integer.

    Args:
        n (float): The float to floor.
    Returns:
        int: The floor of n.
    """
    return int(n)
